FROM python:{{ python_version }}-slim

WORKDIR /app

# Copy project files
COPY . /app/

# Install the package
RUN pip install --no-cache-dir -e .

# Run the application
CMD ["python", "-m", "{{ project_name }}.main"]
